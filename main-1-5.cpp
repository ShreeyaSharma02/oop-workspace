#include <iostream>
int main() {
    int number = 10;

    std::cout << "Number of even numbers between 1 and " << number << " (inclusive): " << count_evens(number) << std::endl;

    return 0;
}